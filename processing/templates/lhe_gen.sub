# ==============================================================================
# lhe_gen.sub - LHE 生成节点 submit 模板 (通用)
# ==============================================================================
# 支持本地和远程 HTCondor 集群
# 通过环境变量或 DAG VARS 控制路径
# ==============================================================================

Universe = vanilla

Executable = run_lhe_gen.sh
transfer_executable = True
arguments = "$(proxy_bundle_name) $(lhe_bundle_name) $(pool) $(seed) $(min_pt_conia) $(min_pt_bonia) $(min_pt_q) $(unwevt) $(test_mode) $(LOCAL_OUTPUT_BASE)"

Should_Transfer_Files = YES
WhenToTransferOutput = ON_EXIT

Transfer_Input_Files = $(lhe_bundle_path), $(proxy_bundle_path)

transfer_output_files =

Output = lhe_$(pool)_$(seed)_$(Cluster)_$(Process).stdout
Error  = lhe_$(pool)_$(seed)_$(Cluster)_$(Process).stderr
Log    = lhe_$(pool)_$(seed)_$(Cluster)_$(Process).log

request_cpus = $(request_cpus)
request_memory = $(request_memory)
request_disk = $(request_disk)

requirements = (TARGET.Machine == "nd-16.hepthu.com")
getenv = True
initialdir = $(LOCAL_LOG_DIR)

environment = "LOCAL_OUTPUT_BASE=$(LOCAL_OUTPUT_BASE)"

+MaxRuntime = 604800

# 容器配置
+SingularityImage = "/cvmfs/unpacked.cern.ch/registry.hub.docker.com/cmssw/el7:x86_64"

OnExitHold = (ExitCode != 0)
OnExitHoldReason = "LHE generation failed"
OnExitHoldSubCode = 1
PeriodicRelease = (JobRunCount < 2) && (HoldReasonCode == 3)

Queue 1